{
  "name": "silentorb-web",
  "description": "Silent Orb Website",
  "author": "Christopher W. Johnson",
  "private": true,
  "scripts": {
    "content:sync": "bun -e \"import { openContentGraph } from 'tome-db/content'; const { db } = openContentGraph(process.env.TOME_CONTENT_PATH ?? 'content', process.env.TOME_DB_PATH ?? 'data/tome.sqlite'); db.close(); console.log('Synced content → data/tome.sqlite');\"",
    "web:build": "bash scripts/build-static-site.sh",
    "web:serve": "bash scripts/serve-static-site.sh"
  }
}
