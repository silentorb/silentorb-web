<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{{title}}}</title>
  <meta charSet="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=5, shrink-to-fit=no"/>
  <link rel="stylesheet" href="/styles.css">
  {{#each headers}}
    {{{this}}}
  {{/each}}
</head>
<body>
<main>
  {{> header }}
  <narrow>
    <article{{#if articleStyle}} class="{{articleStyle}}"{{/if}}>
      {{> content }}
      {{{content}}}
    </article>
  </narrow>

  {{#unless no-footer}}
    {{> footer }}
  {{/unless}}
</main>
</body>
</html>
